$('#banner').slick();
/*{
    // dots: true,
    // autoplay: true,
    // autoplaySpeed: 2000,
    // cssEase: 'ease-in-out'
} */
$('#news-card-wrap').slick({
    slidesToShow: 3,
    cssEase: 'ease-in-out'
});